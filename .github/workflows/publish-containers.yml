name: Publish Containers

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      registry:
        description: "Target container registry"
        required: true
        default: "both"
        type: choice
        options:
          - "both"
          - "dockerhub"
          - "ghcr"
      tag_name:
        description: "Custom tag (leave blank to use git ref)"
        required: false
        default: ""
        type: string
      push:
        description: "Push images to registry (false for dry-run)"
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-containers-${{ github.ref }}
  cancel-in-progress: true

env:
  DOCKER_BUILDKIT: "1"

jobs:
  build-and-publish:
    name: Build & Publish Images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Check Docker Hub credentials
        id: dockerhub-check
        run: |
          if [ -n "${{ secrets.DOCKERHUB_USERNAME }}" ] && [ -n "${{ secrets.DOCKERHUB_TOKEN }}" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to Docker Hub
        if: ${{ steps.dockerhub-check.outputs.available == 'true' && (github.event_name == 'push' || inputs.registry == 'dockerhub' || inputs.registry == 'both') }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        if: ${{ github.event_name == 'push' || inputs.registry == 'ghcr' || inputs.registry == 'both' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags and target registries
        id: meta-calc
        run: |
          REF_TAG="${{ github.ref_name }}"
          INPUT_TAG="${{ inputs.tag_name }}"
          TAG="${INPUT_TAG:-$REF_TAG}"
          CLEAN_TAG="${TAG#v}"
          
          # Convert repository owner and dockerhub user to lowercase for valid OCI image naming
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          DOCKERHUB_USER_LOWER=$(echo "${{ secrets.DOCKERHUB_USERNAME }}" | tr '[:upper:]' '[:lower:]')
          
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "clean_tag=${CLEAN_TAG}" >> "$GITHUB_OUTPUT"
          echo "owner_lower=${OWNER_LOWER}" >> "$GITHUB_OUTPUT"
          echo "dockerhub_user=${DOCKERHUB_USER_LOWER}" >> "$GITHUB_OUTPUT"
          
          SHOULD_PUSH="${{ github.event_name == 'push' || inputs.push }}"
          echo "should_push=${SHOULD_PUSH}" >> "$GITHUB_OUTPUT"

          ENABLE_DOCKERHUB="false"
          if [ "${{ steps.dockerhub-check.outputs.available }}" = "true" ] && [ "${{ github.event_name }}" = "push" -o "${{ inputs.registry }}" = "dockerhub" -o "${{ inputs.registry }}" = "both" ]; then
            ENABLE_DOCKERHUB="true"
          fi
          echo "enable_dockerhub=${ENABLE_DOCKERHUB}" >> "$GITHUB_OUTPUT"

          ENABLE_GHCR="false"
          if [ "${{ github.event_name }}" = "push" -o "${{ inputs.registry }}" = "ghcr" -o "${{ inputs.registry }}" = "both" ]; then
            ENABLE_GHCR="true"
          fi
          echo "enable_ghcr=${ENABLE_GHCR}" >> "$GITHUB_OUTPUT"

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: ${{ steps.meta-calc.outputs.should_push == 'true' }}
          tags: |
            ${{ steps.meta-calc.outputs.enable_dockerhub == 'true' && format('{0}/rag-llm-services-api:{1}', steps.meta-calc.outputs.dockerhub_user, steps.meta-calc.outputs.tag) || '' }}
            ${{ steps.meta-calc.outputs.enable_dockerhub == 'true' && format('{0}/rag-llm-services-api:latest', steps.meta-calc.outputs.dockerhub_user) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-api:{1}', steps.meta-calc.outputs.owner_lower, steps.meta-calc.outputs.tag) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-api:latest', steps.meta-calc.outputs.owner_lower) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-api:sha-{1}', steps.meta-calc.outputs.owner_lower, github.sha) || '' }}

      - name: Build and push Worker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/worker/Dockerfile
          push: ${{ steps.meta-calc.outputs.should_push == 'true' }}
          tags: |
            ${{ steps.meta-calc.outputs.enable_dockerhub == 'true' && format('{0}/rag-llm-services-worker:{1}', steps.meta-calc.outputs.dockerhub_user, steps.meta-calc.outputs.tag) || '' }}
            ${{ steps.meta-calc.outputs.enable_dockerhub == 'true' && format('{0}/rag-llm-services-worker:latest', steps.meta-calc.outputs.dockerhub_user) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-worker:{1}', steps.meta-calc.outputs.owner_lower, steps.meta-calc.outputs.tag) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-worker:latest', steps.meta-calc.outputs.owner_lower) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-worker:sha-{1}', steps.meta-calc.outputs.owner_lower, github.sha) || '' }}

      - name: Build and push Web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: ${{ steps.meta-calc.outputs.should_push == 'true' }}
          tags: |
            ${{ steps.meta-calc.outputs.enable_dockerhub == 'true' && format('{0}/rag-llm-services-web:{1}', steps.meta-calc.outputs.dockerhub_user, steps.meta-calc.outputs.tag) || '' }}
            ${{ steps.meta-calc.outputs.enable_dockerhub == 'true' && format('{0}/rag-llm-services-web:latest', steps.meta-calc.outputs.dockerhub_user) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-web:{1}', steps.meta-calc.outputs.owner_lower, steps.meta-calc.outputs.tag) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-web:latest', steps.meta-calc.outputs.owner_lower) || '' }}
            ${{ steps.meta-calc.outputs.enable_ghcr == 'true' && format('ghcr.io/{0}/rag-llm-services-web:sha-{1}', steps.meta-calc.outputs.owner_lower, github.sha) || '' }}
