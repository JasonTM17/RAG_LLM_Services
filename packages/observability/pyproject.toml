[project]
name = "rag-llm-services-observability"
version = "0.1.0"
description = "Structured JSON logging, secret redaction, and request context for RAG LLM Services."
authors = [{ name = "Nguyen Tien Son" }]
license = "MIT"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_llm_services_observability"]
